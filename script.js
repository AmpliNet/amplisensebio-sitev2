(() => { const dialog=document.querySelector('.dialog'); const toast=m=>{const e=document.createElement('div');e.className='toast';e.textContent=m;document.body.append(e);setTimeout(()=>e.remove(),3600)}; document.querySelector('[data-action="story"]')?.addEventListener('click',()=>dialog.showModal());document.querySelectorAll('[data-action="close"]').forEach(e=>e.addEventListener('click',()=>dialog.close()));dialog?.addEventListener('click',e=>{if(e.target===dialog)dialog.close()});document.querySelector('[data-action="protect"]')?.addEventListener('click',()=>toast('Cenário V3: a janela foi protegida como proposta. No produto, você revisa e confirma antes de alterar seu calendário.'));document.querySelector('[data-action="evidence"]')?.addEventListener('click',()=>toast('Evidência demonstrativa: calendário, prioridade declarada e cobertura de sinal — sem inferência clínica.'));document.querySelector('[data-action="policy"]')?.addEventListener('click',()=>toast('Cenário V3: política criada como experimento de coorte, sujeita a revisão antes de publicação.'));})();
